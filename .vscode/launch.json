{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug via Chromium",
            "type": "chrome",
            "request": "launch",
            "file": "${file}",
            "webRoot": "${workspaceFolder}"
        }
    ]
}